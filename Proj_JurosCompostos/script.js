function gerarTabuada() {
    let valorInicial = document.getElementById("val-inic").value;
    let investMensal = document.getElementById("invest-men").value;
    let escrita = document.getElementById("select-estilo").value;
    let cor = document.getElementById("select-cor").value;

    // let linhaTabuada = "";

    // alert(numero + " | " + mult + " | " + escrita + " | " + cor);
    // document.getElementById("tabuada-resultado").style.color = cor;
    // if (escrita == "textual") {
        // for(contagem = 0; contagem <= mult; contagem++) {

            // linhaTabuada += "<div>" + numero + " multiplicado por " + contagem + " é igual a: " + (numero * contagem) + "</div>";
        // }
    // } else {
        // for(contagem = 0; contagem <= mult; contagem++) {

            // linhaTabuada += "<div>" +numero + " x " + contagem + " = " + (numero * contagem) + "</div>";
        // }
    // }
    

    // document.getElementById("section-tabuada").style.display="block";
    // document.getElementById("tabuada-resultado").innerHTML = linhaTabuada;

}